{
  "name": "@adexchange/aeg-logger",
  "version": "0.0.36",
  "description": "Configurable logger",
  "main": "./lib/logger.js",
  "author": "Justin Durham",
  "repository": {
    "type": "git",
    "url": "https://github.com/AdExchangeGrp/aeg-logger.git"
  },
  "license": "UNLICENSED",
  "scripts": {
    "start": "node index.js",
    "test": "mocha --recursive --compilers js:babel-core/register test/",
    "test:coverage": "istanbul cover _mocha -- --recursive --compilers js:babel-core/register test/",
    "eslint": "eslint .",
    "eslint:watch": "npm run eslint && onchange \"**/*.js\" \".eslint*\" -- npm run eslint",
    "flow": "flow check",
    "compile": "babel -d ./lib/ ./src/ && flow-copy-source ./src ./lib",
    "compile:watch": "npm run compile -- --watch",
    "dev": "npm run compile:watch",
    "build": "npm run eslint && npm run flow && npm run compile && istanbul cover _mocha -- --recursive --compilers js:babel-core/register --reporter mocha-junit-reporter test/unit",
    "patch": "git reset --hard HEAD && git clean -fd && npm run compile && npm version patch -m \"[ci skip] %s\" && git push origin master && git push origin master --tags && npm publish"
  },
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-core": "^6.23.1",
    "babel-eslint": "^7.1.1",
    "babel-plugin-transform-builtin-extend": "^1.1.2",
    "babel-plugin-transform-flow-strip-types": "^6.22.0",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-latest": "^6.22.0",
    "eslint": "^3.16.1",
    "eslint-config-standard": "^6.2.1",
    "eslint-plugin-promise": "^3.4.2",
    "eslint-plugin-standard": "^2.0.1",
    "flow-bin": "^0.40.0",
    "flow-copy-source": "^1.1.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.2.0",
    "mocha-junit-reporter": "^1.13.0",
    "onchange": "^3.2.1",
    "should": "^11.2.0"
  },
  "dependencies": {
    "babel-runtime": "^6.23.0",
    "config": "^1.25.1",
    "js-yaml": "^3.8.1",
    "lodash": "^4.17.4",
    "winston": "^2.3.1",
    "winston-error": "^1.0.1",
    "winston-papertrail": "^1.0.4"
  },
  "engine": "node >= 6.10.0"
}
