language: node_js
cache: true
node_js:
  - "0.10"
# exclude
branches:
  except:
    - dev

# include
branches:
  only:
    - master
script:
    - npm -g install jshint
    - npm run jshint
    - npm test
